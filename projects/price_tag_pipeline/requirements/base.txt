# Core runtime — everything the inference pipeline needs to load and run.
# Recommended Python: 3.11 or 3.12 (broadest ecosystem support for ultralytics, paddleocr, torch).

numpy>=1.24,<2.3
opencv-python>=4.9,<5.0
Pillow>=10.0
PyYAML>=6.0
scikit-learn>=1.4         # video-level GroupKFold / StratifiedKFold
ultralytics>=8.3          # YOLO11/12/26, ByteTrack, BoT-SORT
tqdm>=4.66                # inference progress bar (import-guarded — soft dep)
